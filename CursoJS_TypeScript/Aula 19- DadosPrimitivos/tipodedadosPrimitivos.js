// Strin, number, undefined, null, boolean

//exemplos de string
const nome1 = 'luiz';
const nome2 = "luiz";
const nome3 = `luiz`;

// number
const num1 = 10;
const num2 = 10.52;

//undefined ... nao aponta para local nenhum na memoria, nulo
let nomeAluno;

// Nulo.. nao aponta para lugar nenhum na memoria, porem é diferente do undefined, pq essa variavel pode nao ter valor nenhum. ex escolha de cor, ele pode escolher ou deixar em nulo.
let sobrenomeAluno = null; //pode ser CONST

//false   BOOLEAN = TRUE , FALSE (LOGICO)
const boolean = true; 
const aprovado = true;

//array
const a = [1,2];
const b = a;
console.log(a,b)




//para ver o tipo das variaveis e constante
console.log(typeof sobrenomeAluno);
